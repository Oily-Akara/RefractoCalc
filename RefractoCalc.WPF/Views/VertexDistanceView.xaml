<UserControl x:Class="RefractoCalc.WPF.Views.VertexDistanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:RefractoCalc.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- 1. Connect the ViewModel -->
    <UserControl.DataContext>
        <vm:VertexDistanceViewModel/>
    </UserControl.DataContext>

    <Grid Background="#F0F0F0">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">

            <TextBlock Text="Vertex Distance Calculator" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <!-- INPUT: Original Power -->
            <Label Content="Original Power (Diopters)"/>
            <TextBox Text="{Binding OriginalPower, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,10" Padding="5"/>

            <!-- INPUT: Old Vertex -->
            <Label Content="Current Vertex Distance (mm)"/>
            <TextBox Text="{Binding OldVertexMm, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,10" Padding="5"/>

            <!-- INPUT: New Vertex -->
            <Label Content="Target Vertex Distance (mm)"/>
            <TextBox Text="{Binding NewVertexMm, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,20" Padding="5"/>

            <!-- ACTION: Button -->
            <!-- The Command binds to the [RelayCommand] inside the ViewModel -->
            <Button Content="Calculate" 
                    Command="{Binding CalculateCommand}"
                    Height="40" Background="#007ACC" Foreground="White" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- OUTPUT: Result -->
            <TextBlock Text="Required Power:" HorizontalAlignment="Center" Foreground="Gray"/>
            <TextBlock Text="{Binding ResultPower, StringFormat=N2}" 
                       FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#333"/>
            <TextBlock Text="Diopters" HorizontalAlignment="Center" FontSize="10"/>

        </StackPanel>
    </Grid>
</UserControl>