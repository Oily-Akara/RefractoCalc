<UserControl x:Class="RefractoCalc.WPF.Views.ACDView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:RefractoCalc.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:ACDViewModel/>
    </UserControl.DataContext>

    <Grid Background="#F0F0F0">
        <StackPanel Margin="20">
            <TextBlock Text="Anterior Chamber Depth Estimator" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
            <TextBlock Text="⚠️ Estimation only. Use optical biometry for surgical planning." Foreground="Red" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TabControl Height="300" Width="500">

                <!-- TAB 1: SLIT LAMP -->
                <TabItem Header="Slit-Lamp (Smith)">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
                        <TextBlock Text="Just-Touching Slit Length (mm)" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SlitLength}" Height="30" Margin="0,0,0,20" Padding="5"/>

                        <Button Content="Calculate" Command="{Binding CalcSlitCommand}" Height="35" Background="#007ACC" Foreground="White" FontWeight="Bold"/>

                        <TextBlock Text="Estimated ACD:" Margin="0,20,0,0" HorizontalAlignment="Center" Foreground="Gray"/>
                        <TextBlock Text="{Binding ResultSlit, StringFormat=N2}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="mm" HorizontalAlignment="Center"/>
                    </StackPanel>
                </TabItem>

                <!-- TAB 2: PHOTO -->
                <TabItem Header="Photo (EZ-Ratio)">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
                        <TextBlock Text="Distance E (Pixels)" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding PhotoE}" Height="30" Margin="0,0,0,10" Padding="5"/>

                        <TextBlock Text="Distance Z (Pixels)" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding PhotoZ}" Height="30" Margin="0,0,0,20" Padding="5"/>

                        <Button Content="Calculate" Command="{Binding CalcPhotoCommand}" Height="35" Background="#007ACC" Foreground="White" FontWeight="Bold"/>

                        <TextBlock Text="Estimated ACD:" Margin="0,20,0,0" HorizontalAlignment="Center" Foreground="Gray"/>
                        <TextBlock Text="{Binding ResultPhoto, StringFormat=N2}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="mm" HorizontalAlignment="Center"/>
                    </StackPanel>
                </TabItem>

                <!-- TAB 3: POST-OP PREDICTION -->
                <TabItem Header="Post-Op Prediction">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
                        <TextBlock Text="Pre-Op ACD (mm)" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding PreOpAcd}" Height="30" Margin="0,0,0,20" Padding="5"/>

                        <Button Content="Calculate" Command="{Binding CalcPostOpCommand}" Height="35" Background="#007ACC" Foreground="White" FontWeight="Bold"/>

                        <TextBlock Text="Predicted Post-Op ACD:" Margin="0,20,0,0" HorizontalAlignment="Center" Foreground="Gray"/>
                        <TextBlock Text="{Binding ResultPostOp, StringFormat=N2}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="mm" HorizontalAlignment="Center"/>
                    </StackPanel>
                </TabItem>

            </TabControl>
        </StackPanel>
    </Grid>
</UserControl>